import React from "react";
import { Routes, Link, Route } from "react-router-dom";

const NewBeer = () => {
  return (
    <div>
      <nav>
        <Link to="/">Home</Link>
      </nav>
    </div>
  );
};

export default NewBeer;
